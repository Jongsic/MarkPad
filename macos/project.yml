name: MarkPad
options:
  bundleIdPrefix: com.jongsic
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

packages:
  swift-markdown:
    url: https://github.com/swiftlang/swift-markdown
    from: "0.4.0"

settings:
  base:
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: 1.3.1
    CURRENT_PROJECT_VERSION: 4
    CODE_SIGN_STYLE: Manual
    CODE_SIGN_IDENTITY: "-"

targets:
  MarkPad:
    type: application
    platform: macOS
    sources:
      - Sources
      - Resources
    dependencies:
      - package: swift-markdown
        product: Markdown
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.jongsic.MarkPad
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ENABLE_HARDENED_RUNTIME: YES
    info:
      path: Resources/Info.plist
      properties:
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        CFBundleName: MarkPad
        CFBundleDisplayName: MarkPad
        NSPrincipalClass: NSApplication
        NSHumanReadableCopyright: "MIT License © 2026 Jongsic and MarkPad contributors"
        LSApplicationCategoryType: public.app-category.productivity
        NSSupportsAutomaticTermination: true
        NSSupportsSuddenTermination: false
        CFBundleDocumentTypes:
          - CFBundleTypeName: Markdown Document
            CFBundleTypeRole: Editor
            LSHandlerRank: Default
            LSItemContentTypes:
              - net.daringfireball.markdown
            NSDocumentClass: MarkPad.MarkdownDocument
        UTImportedTypeDeclarations:
          - UTTypeIdentifier: net.daringfireball.markdown
            UTTypeDescription: Markdown Document
            UTTypeConformsTo:
              - public.plain-text
            UTTypeTagSpecification:
              public.filename-extension:
                - md
                - markdown
                - mdown
                - mkd

  MarkPadTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests
    dependencies:
      - target: MarkPad
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES

schemes:
  MarkPad:
    build:
      targets:
        MarkPad: all
        MarkPadTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - MarkPadTests
